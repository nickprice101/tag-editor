## Summary
- move Debian package installation and Playwright setup from container startup into image build
- update the README compose example to build from the repo `Dockerfile`
- keep runtime restarts lightweight so app code changes still work via the bind mount

## Testing
- not run (documentation and container build config change only)
