## Summary
- align the container working directory with the flat `/opt/beets/tag-editor` NAS deployment path
- install Python dependencies from a root-level `requirements.txt` so Portainer builds do not depend on a `site/` subdirectory
- document the expected flat on-disk layout for Portainer and compose deployments

## Testing
- not run (`docker` is not available in this workspace for compose validation)
